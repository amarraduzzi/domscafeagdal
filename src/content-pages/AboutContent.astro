---
// See HomeContent.astro's top comment for the shared-body-per-route
// pattern this and every other src/content-pages/*.astro file follows.
import Layout from '../layouts/Layout.astro';
import PageHero from '../components/PageHero.astro';
import Ornament from '../components/Ornament.astro';
import Button from '../components/Button.astro';
import { siteConfig } from '../config/site.config';
import { aggregateRating, testimonials } from '../content/testimonials';
import { t, localizedPath, type Language } from '../i18n/languages';
import { common, place, nav } from '../i18n/ui';

interface Props {
  lang?: Language;
}
const { lang = 'fr' } = Astro.props;

const { name } = siteConfig.identity;
const primaryKeyword = t(siteConfig.identity.primaryKeyword, lang);
const city = t(place.city, lang);

const copy = {
  metaTitle: {
    fr: `À propos, ${name}, ${primaryKeyword} à ${city}`,
    en: `About, ${name}, ${primaryKeyword} in ${city}`,
    ar: `من نحن, ${name}, ${primaryKeyword} في ${city}`,
  },
  metaDescription: {
    fr: `L'histoire de ${name}, ${primaryKeyword} à ${city} : une adresse familiale au cœur d'Agdal, boissons et plats faits maison.`,
    en: `The story of ${name}, ${primaryKeyword} in ${city}: a neighborhood spot in the heart of Agdal, homemade drinks and dishes.`,
    ar: `قصة ${name}, ${primaryKeyword} في ${city}: عنوان عائلي في قلب أكدال، مشروبات وأطباق منزلية الصنع.`,
  },
  kicker: { fr: 'Notre histoire', en: 'Our story', ar: 'قصتنا' },
  heroTitle: { fr: `À propos de ${name}`, en: `About ${name}`, ar: `عن ${name}` },
  heroAlt: {
    fr: `Intérieur de ${name}`,
    en: `Interior of ${name}`,
    ar: `داخل ${name}`,
  },
  p1: {
    fr: `${name} est une adresse installée du quartier Agdal, à ${city} : un ${primaryKeyword} où l'on passe aussi bien pour un café rapide que pour un repas complet, à toute heure de la journée.`,
    en: `${name} is an established spot in the Agdal neighborhood of ${city}: a ${primaryKeyword} for a quick coffee as much as a full meal, any time of day.`,
    ar: `${name} عنوان راسخ في حي أكدال بمدينة ${city}: ${primaryKeyword} يُقصد لتناول قهوة سريعة أو وجبة كاملة، في أي وقت من اليوم.`,
  },
  p2: {
    fr: "Boissons chaudes et fraîches, petits-déjeuners, crêpes, pizzas, sandwiches, pâtes et desserts : tout est préparé maison, à la commande, jamais réchauffé.",
    en: 'Hot and cold drinks, breakfasts, crêpes, pizzas, sandwiches, pasta and desserts: everything is made in-house, to order, never reheated.',
    ar: 'مشروبات ساخنة وباردة، فطور، كريب، بيتزا، سندويشات، معكرونة وحلويات: كل شيء يُحضَّر في المكان، عند الطلب، دون تسخين.',
  },
  p3: {
    fr: "Les commandes, sur place, à emporter ou en livraison, passent par notre système en ligne, relié directement à la cuisine. C'est cette régularité, plus que n'importe quel argument marketing, qui fait revenir nos clients.",
    en: 'Orders, whether dine-in, takeaway or delivery, go through our online system, connected straight to the kitchen. It\'s this consistency, more than any marketing argument, that keeps our customers coming back.',
    ar: 'الطلبات، في المقهى أو للاستلام أو للتوصيل، تمر عبر نظامنا الإلكتروني، المرتبط مباشرة بالمطبخ. هذا الانتظام، أكثر من أي حجة تسويقية، هو ما يجعل زبائننا يعودون دائمًا.',
  },
  statAns: { fr: "au cœur d'Agdal", en: 'in the heart of Agdal', ar: 'في قلب أكدال' },
  statPlats: { fr: 'plats faits maison', en: 'homemade dishes', ar: 'أطباق منزلية الصنع' },
  statAvis: { fr: 'avis Google', en: 'Google reviews', ar: 'تقييم على Google' },
  noteMoyenne: { fr: 'note moyenne', en: 'average rating', ar: 'متوسط التقييم' },
  // "Où nous trouver" section — client feedback: page felt too bare below
  // the stats band. Reuses the exact same address/hours/map pattern as
  // ContactContent.astro (see that file) rather than a lighter duplicate,
  // so someone convinced by the story here doesn't have to leave the page
  // to check "is it open now / where exactly".
  findUsTitle: { fr: 'Nous trouver', en: 'Find us', ar: 'كيف تجدنا' },
} satisfies Record<string, { fr: string; en: string; ar: string }>;

const stars = (n: number) => '★'.repeat(n) + '☆'.repeat(5 - n);
// Featured quote here is deliberately a different review than the one
// Avis Clients leads with (testimonials[0]) — repeating the same pull-quote
// verbatim on two pages would read as padding, not as more social proof.
const featuredReview = testimonials[1] ?? testimonials[0];

const path = localizedPath('/a-propos', lang);
---
<Layout
  title={t(copy.metaTitle, lang)}
  description={t(copy.metaDescription, lang)}
  path={path}
  lang={lang}
>
  <PageHero
    kicker={t(copy.kicker, lang)}
    title={t(copy.heroTitle, lang)}
    image="/images/dom/preview/ai-preview-booth.webp"
    alt={t(copy.heroAlt, lang)}
  />
  <section class="relative max-w-3xl mx-auto px-5 sm:px-8 pt-12 sm:pt-16 pb-20 overflow-hidden">
    <!-- Large faded emblem behind the copy — the real logo, not a
         generic decorative shape, used at a scale it can never appear at
         in the header. A quiet "wow" texture instead of another flat
         paragraph block. -->
    <img
      src="/images/dom/logo.webp"
      alt=""
      aria-hidden="true"
      class="mark-watermark w-[26rem] h-[26rem] -right-32 -top-16 sm:-right-24 object-contain"
    />
    <div class="relative space-y-5 text-sm sm:text-base leading-relaxed" style="color: var(--color-text-muted)" data-reveal>
      <p>{t(copy.p1, lang)}</p>
      <p>{t(copy.p2, lang)}</p>
      <p>{t(copy.p3, lang)}</p>
    </div>
  </section>

  <!-- Oversized, unboxed numbers instead of three identical grey tiles.
       First pass of this looked fine on desktop's 3-column grid but flat
       on mobile — a border-left divider does nothing once the grid stacks
       to one column, so it was just three plain lines of text in a row.
       Fixed: each stat gets its own small icon (so it reads as a distinct
       moment, not just a number), the divider switches to a horizontal
       rule on mobile / vertical rule on desktop, and a soft red glow sits
       behind the whole band so it isn't just another flat tinted box. -->
  <div class="section-tint relative overflow-hidden">
    <div class="section-glow" style="width: 340px; height: 340px; background: var(--color-primary); opacity: 0.07; left: 50%; top: -120px; transform: translateX(-50%);"></div>
    <section class="relative max-w-3xl mx-auto px-5 sm:px-8 py-16 sm:py-20">
      <div class="grid sm:grid-cols-3 gap-10 sm:gap-6">
        {[
          {
            n: 'Agdal',
            l: t(copy.statAns, lang),
            icon: 'M12 2c-1 3-4 4-4 8a4 4 0 008 0c0-4-3-5-4-8zM6 20h12',
          },
          {
            n: '100%',
            l: t(copy.statPlats, lang),
            icon: 'M5 21V9a7 7 0 0114 0v12M5 21h14M9 21v-6h6v6',
          },
          {
            n: `${aggregateRating.ratingValue}/5`,
            l: `${t(copy.noteMoyenne, lang)} · ${aggregateRating.reviewCount} ${t(copy.statAvis, lang)}`,
            icon: 'M12 2.5l2.9 6.1 6.6.7-4.9 4.6 1.3 6.6L12 17.3l-5.9 3.2 1.3-6.6-4.9-4.6 6.6-.7z',
          },
        ].map((stat, i) => (
          <div
            class={`text-center sm:text-left pb-8 sm:pb-0 sm:pl-8 ${i > 0 ? 'border-t sm:border-t-0 sm:border-l pt-8 sm:pt-0' : ''}`}
            style={`border-color: var(--color-border); transition-delay: ${i * 100}ms`}
            data-reveal
          >
            <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke-width="1.5" class="mb-3 mx-auto sm:mx-0" style="color: var(--color-accent)">
              <path d={stat.icon} stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" />
            </svg>
            <p class="stat-huge">{stat.n}</p>
            <p class="text-xs mt-2 max-w-[14rem] mx-auto sm:mx-0" style="color: var(--color-text-muted)">{stat.l}</p>
          </div>
        ))}
      </div>
    </section>
  </div>

  <!-- Real, sourced Google review (see src/content/testimonials.ts) as an
       unboxed pull-quote — same treatment as Avis Clients' lead quote, but
       a different review (see featuredReview above) so the two pages don't
       just repeat each other. -->
  {featuredReview && (
    <section class="relative max-w-3xl mx-auto px-5 sm:px-8 pt-16 sm:pt-20 pb-4 text-center overflow-hidden" data-reveal>
      <div class="section-glow" style="width: 360px; height: 360px; background: var(--color-accent); opacity: 0.08; left: -120px; top: -40px;"></div>
      <span class="relative font-display block text-6xl leading-none mb-2" style="color: var(--color-accent)">"</span>
      <p class="relative pull-quote -mt-6">{t(featuredReview.text, lang)}</p>
      <p class="relative mt-6 text-sm font-bold" style="color: var(--color-accent)">
        {featuredReview.name} · {stars(featuredReview.rating)}
      </p>
    </section>
  )}

  <Ornament />

  <!-- Opening hours + map, same pattern/markup as ContactContent.astro's
       equivalent block (kept intentionally lighter here: no phone/WhatsApp
       rows, since Contact already owns that — this is just "convinced by
       the story, when can I come by"). -->
  <section class="max-w-4xl mx-auto px-5 sm:px-8 pb-20 sm:pb-24">
    <h2 class="font-display text-2xl sm:text-3xl font-semibold mb-6 text-center" style="color: var(--color-text-primary)" data-reveal>
      {t(copy.findUsTitle, lang)}
    </h2>
    <div class="grid sm:grid-cols-2 gap-8 items-center">
      <div class="space-y-4 text-sm sm:text-base text-center sm:text-left" style="color: var(--color-text-primary)" data-reveal>
        <p>
          <span class="font-bold text-xs uppercase tracking-wide block" style="color: var(--color-text-muted)">{t(common.adresse, lang)}</span>
          {siteConfig.contact.address}
        </p>
        <p>
          <span class="font-bold text-xs uppercase tracking-wide block" style="color: var(--color-text-muted)">{t(common.horaires, lang)}</span>
          {t(siteConfig.contact.openingHours, lang)}
        </p>
        <div class="flex flex-wrap justify-center sm:justify-start gap-3 pt-2">
          {siteConfig.contact.mapsUrl && (
            <Button href={siteConfig.contact.mapsUrl} external variant="filled">{t(common.itineraire, lang)}</Button>
          )}
          <Button href={localizedPath('/contact', lang)} variant="outline">{t(nav.contact, lang)}</Button>
        </div>
      </div>
      <div
        class="rounded-2xl overflow-hidden aspect-[4/3]"
        style="background-color: color-mix(in srgb, var(--color-primary-dark) 10%, var(--color-surface)); border: 1px solid var(--color-border)"
        data-reveal
      >
        <iframe
          src={`https://www.google.com/maps?q=${encodeURIComponent(siteConfig.contact.address)}&output=embed`}
          class="w-full h-full grayscale-[35%] contrast-[1.05]"
          style="border: 0; min-height: 260px;"
          loading="lazy"
          referrerpolicy="no-referrer-when-downgrade"
          title={`${name}, ${siteConfig.contact.address}`}
        ></iframe>
      </div>
    </div>
  </section>
</Layout>
